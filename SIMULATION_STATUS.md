# 模拟运行断点验收

本次保留原虚拟账户，未重置本金、持仓、成交或结束时间。
修复前完成 94 轮，失败 2 轮；5 笔建仓、2 笔回本卖出。

## 已核验

- SQLite 关闭再打开，状态一致。
- 从全部模拟成交重算现金，结果一致。
- 从全部模拟成交重算 5 个持仓数量，结果一致。
- 修复数据源整体失败时旧净值仍显示的问题：净值/浮盈亏置 null，保留已成交账本。
- 模拟成交通知改从持久账本扫描并按稳定键去重；历史 7 条已成功推送键已迁移，不重复发送。
- 旧进程已停止，启动新进程继续剩余时长；仍以原定 24 小时结束时间为准。

## 仍需明确的限制

当前是纸面成交模型，不是实盘执行器。行情报价可用不等于真实钱包一定能卖出。
没有单币亏损退出规则；5 个持仓上限可能被低价值仓位占满。这是现有策略行为，本次不擅自改变它。
回本和 10 倍半仓规则运行中；半仓尚无真实行情触发记录，只有关键用例验证。
持仓数据库恢复已验证，不等于签名交易的崩溃恢复已完成。
费用采用模型估算，完整手续费和滑点需实盘前另外核验。

当前状态见 data/sim-run/status.json，持仓和模拟成交见 data/paper.sqlite。
