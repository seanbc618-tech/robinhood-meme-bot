# v4 精简复验

通过部分：repair验证通过；只读run 49完成/35失败，最新cycle5412ms、exits2209ms；17个相邻tick间隔除一个32226ms外约60.0–60.2s。注意这是worker tick起点，不是每次实际完成持仓退出的证明。有效USD桶仍只有1根，不能进入经济评估。

当前根因不能只归于汇率供应商：
- pickLiveWatch按最新排序返回8池，但cycle仅watch.live.slice(0,ANALYZE_LIMIT)，没有轮转。若limit=3，后5池长期不处理。last_analyzed_at不影响这个排序。
- 每池每次至多300块，而约10块/秒意味着每60秒新增约600块，即使每轮都处理仍落后。超过阈值再跳head-300会周期性制造缺口，无法持续完整窗口。必须按实测链增长率和采集能力核算，不继续只调小chunk。
- 最新池持续替换观察池，A需2小时、B需24小时；滚动最新8池可能在达到年龄前就被剔除。固定观察队列生命周期，留足B年龄和120分钟窗口，满额时新池排队，明确披露覆盖范围；不暗改策略年龄。

下一步限定为采集闭环修复，不改策略或放宽FX：先固定少量观察池（可从1池验证再扩容）并保留其到期/生命周期；区分单RPC chunk大小与每轮总覆盖量，保证覆盖增长>=链增长；每分钟保存当时源FX用于对应窗口而非回补时新拉的FX。逐桶输出缺失原因：未采集、队列未服务、跳过积压、observed_at滞后、source last_updated滞后，才能判断供应商问题。

验收：真实固定池连续30分钟有效桶 + 明确无gap + 退出tick稳定；随后继续到A/B所需年龄与窗口。合成30桶仅证明函数，不证明生产有效采集。保留数据/账户/结束时间，无真实交易。本次未修改运行程序。
