# v3 独立复验：性能改善，运行验收未通过

现有 repair 检查通过。只读快照：PID字段84435/code_version v3；最后完成cycle 34417ms，exits 5044ms；30完成/33失败。有效记录中2275根usd_usable=0、16根invalid，usd_usable=1为0。177个支持池的事件游标距最后完成head落后7750～245025块。

必须解决的四点：
1. abc.mjs worker 在每轮完成后 Date.now()+60000 才开始下一轮，因此34秒cycle意味着约94秒决策周期，49秒则109秒。以轮开始时间计算下一次截止，记录真实相邻退出开始间隔；超时不重叠启动多轮。
2. 每池800块、每轮3池、177池轮转且仍追旧游标，使前向策略窗口无法产生。当前0根usd_usable=1，不能称A/B/C正在有效模拟。需核算实测链增长率/每池服务率与积压，分开历史回补和当前订阅：保存断档原貌，缺失窗口不可交易；向前新覆盖边界明确标记，不补造数据、不把跳过历史伪装完整。可用小且明确披露的固定观察队列验证实时采集，但不得暗缩样本或宣称全目录覆盖。不要放宽费用/信号/新鲜度来造交易。
3. 35秒预算仅在池/日志chunk入口判断，chunk内部逐条await blockTs没有总预算；foldStoredEvents、enrichPool、syncCatalog仍有未受12秒封顶的路径。syncCatalog还在持仓退出之前，报告“先退出”不准确。先处理退出，所有长RPC阶段有明确超时和剩余预算；Promise.race不取消底层任务，特别blockTs会异步写库，需确保超时返回后不会串入后续事务。不能以一轮<60秒代替压力证明。
4. foldStoredEvents 获取last_event_block时间失败被catch{}吞掉，coveredTs退化为head时间；积压日志尚未扫完时可能将未覆盖分钟当无交易关闭。查不到已覆盖边界必须SOURCE_UNAVAILABLE并停止关桶，不能按head兜底。加末端时间查询失败且游标滞后时不产生桶的验证。

补充：fxContemporaneous只比较rates.observed_at而非实际last_updated_at。读到旧汇率并不代表该分钟有同时点源价格；窗口可用性需检查实际源时间和具体报价币所需汇率。

验收证据：临时目录完整worker调度测试；真实至少10个连续轮次相邻退出间隔与每轮延迟；至少一个明确覆盖池连续30根可用于策略的分钟桶（B仍需等足窗口/年龄）；每策略WARMUP/拒绝真实持久化。允许零成交，不允许零可用桶冒充有效实验。原账户/结束时间/旧数据保留；不触碰原模拟或真实交易。

本次只读检查与写本报告，没有修改运行代码、停止进程或交易。
