# ABC v9：持有人历史完整读取与C仓位一致性

用户在2026-09-07复审后授权直接修复。持有人查询不再一次读取创世至当前块：先用历史合约代码二分定位部署块，再以最多5000块一页记录Transfer，保存进度以跨周期续读。注册块只作为部署查找的上界，不作为铸币起点。独立holder-history.sqlite是来源缓存，不含账户。每页校验覆盖结束区块哈希，读取缓存前校验其锚点；发现重组拒绝使用缓存。保留mint、非负余额、供应量、头部地址balanceOf全部校验。删除20000条硬上限。

分钟采集和目录同步之后，在原45秒预算剩余时间内预热一个观察池；请求受剩余时间约束，单轮最多8页。未完整读取不得通过安全筛选。已有信号不补记生产成交。

C新单笔本金上限由15改为30美元，总现金支出上限20改为35，匹配A/B；这会增加C单笔风险敞口。现存账户仅更新这两个限制并记录迁移时间，余额、持仓、交易、实验日期不变；旧仓成本与版本不回写。新交易version和code_version均为abc-phase1-v9。费用模型、5%往返上限、120秒实时新鲜度、持有人阈值及ABC信号规则不变。

验证：原超时的两个实际池完整读取22228及19492条Transfer，总供应量与头部余额对账成功。8条既有信号在隔离账户中通过真实holderData，随后被真实报价的ROUND_TRIP_COST_OVER_5_PERCENT拒绝：30美元本金的损失约8.27%–8.74%。使用桶保存的source_block及当时FX，gas为当前值，属于实现路径诊断，不是严格历史收益重放。未生成生产成交。两个池当前hookFeeBps=100、creatorTaxBps=100；修好读取不意味着报价会符合5%上限。

现有repair/offline/screening三组检查通过。新增回归覆盖部署早于注册、分页失败续读与去重、历史查询排除未来事件、重组锚点拒绝、C迁移幂等及账户历史保留。单个短时RPC对照不能证明长期稳定或盈利能力。
