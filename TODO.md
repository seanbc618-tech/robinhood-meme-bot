# 待修清单

来源：2026-09-23 全面扫描。每条都用生产数据或复现核实过，细节见 `PROJECT_HANDOFF.md` 的 09-23 一节。修完一条就在这里打勾并写上提交号。

## 已修

- [x] **1. 联网排队卡死，程序悄悄退出**：`4205d54`，09-23 部署。一个卡死的回复会让同一节点后面的请求全部永远排队，下一轮开始 6 秒后程序以"成功"退出、systemd 不拉起（09-22 停了 9 小时）。另有 `8c627cf` 兜底：再有类似情况会记 `WORKER_STALLED` 并由 systemd 15 秒内重启。

## 待修（按建议顺序）

- [ ] **2. 数据没收齐就判分钟**
  - 评估按链上最新分钟走，不看这个币的数据收到了哪。采集慢一步时，最新那分钟被当成"缺数据"，正在形成的形态被清零，数据后来到了也不补判。会悄悄吞信号，C 那种要连续 30 分钟的形态受影响最大。
  - 规模：09-14 至 09-20 每天 10–24% 的实时分钟如此（`COVERAGE_NOT_CLOSED`），正常运行时 0%。
  - 修法：每个币只判到它自己已收齐的分钟（`pools.last_complete_minute`），没收齐的留到下一轮。
  - 位置：`abc-runtime.mjs` `cycle()` 里的分钟评估循环。`abc-repair-verify.mjs` 里有一条用例断言了现在的行为，要一起改。

- [ ] **3. 跳过缺口后沿用旧价格**
  - 跳过一段缺口后，没成交的分钟沿用缺口前的最后价格，还标成可用（`usd_usable=1`），等于补造数据。09-23 重跳后 B 座位的 `0x6321652A` 在用 44 小时前的价格。
  - 修法：沿用价格不跨越已记录的缺口；缺口后第一笔成交之前的分钟标为不可用。
  - 位置：`abc-collect.mjs` `foldStoredEvents()`。同一函数里还有两处同类小问题：汇率不可用的分钟不推进沿用价；重试旧的失败分钟会把沿用价拨回更旧的值。

- [ ] **4. 一轮之内不查止损**
  - 每轮 30–45 秒里只在开头查一次止损；"每 15 秒查一次"只在两轮之间成立。
  - 实例：C 在 09-13 那笔，价格穿过止损线 36 秒后才卖（-30.3%，止损线 -20%）。亏得最多的几笔主要是一秒内闪崩，查得再勤也躲不开，这条只能减少一部分超额。
  - 修法：持仓期间，一轮里每处理完一个币就补查一次退出。
  - 位置：`abc-runtime.mjs` `cycle()` 和 `exitOnlyTick()`。

- [ ] **5. 一个价格源失败，整轮作废**
  - Coinbase ×2 + CoinGecko 任一失败（多半是 CoinGecko 限流），整轮报错：这一分钟所有持仓不标价、不查止损，连只用 ETH 计价、根本用不到稳定币价格的池也一样。两轮之间的退出检查同样失败。
  - 修法：只有 ETH 价格是必需的；稳定币价格失败只影响 USDG 计价的池。
  - 位置：`chain.mjs` `usdRates()`（`Promise.all`）；`abc-runtime.mjs` `cycle()` 开头和 `exitOnlyTick()`。

- [ ] **6. 砸盘后仓位可能无法估值（潜在，尚未发生）**
  - 模拟卖出用的钱包名单固定为入场那一刻的前 10 大持有人，只在名单为空时刷新。砸盘后这些钱包可能都不够余额，标价就会一直失败：净值变成空、止损永远不触发、这个账户永久停止买入，亏损也不会记进已实现。
  - 修法：标价失败时刷新一次持有人名单再试。
  - 位置：`abc-entry.mjs` 标价部分，`chain.mjs` `simulateExit()`，`abc-collect.mjs` `netExitValue()`。
