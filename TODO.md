# 待修清单

来源：2026-09-23 全面扫描。每条都用生产数据或复现核实过，细节见 `PROJECT_HANDOFF.md` 的 09-23 一节。修完一条就在这里打勾并写上提交号。

## 已修

- [x] **1. 联网排队卡死，程序悄悄退出**：`4205d54`，09-23 部署。一个卡死的回复会让同一节点后面的请求全部永远排队，下一轮开始 6 秒后程序以"成功"退出、systemd 不拉起（09-22 停了 9 小时）。另有 `8c627cf` 兜底：再有类似情况会记 `WORKER_STALLED` 并由 systemd 15 秒内重启。
- [x] **2. 数据没收齐就判分钟**：`d0d54b0`。每个币只判到它自己的日志已收齐的分钟，没收齐的留到下一轮；信号分钟因此晚一轮判定时，3 分钟内仍可买入（与挂单信号的寿命一致）。
- [x] **3. 跳过缺口后沿用旧价格**：`9615cdb` + `10b9728`。无成交分钟逐分钟查“缺口之后最近一根 K 线”的链上价格，按本分钟汇率折算；缺口后到下一笔成交前的分钟标为缺失；跳点所在的半分钟不再生成。`10b9728` 在 worker 启动时把旧代码已编出的假分钟标为无效（`CARRIED_ACROSS_GAP`），上线前只读演算：22 个币共 2016 分钟，其中近 8 小时 31 分钟。
- [x] **4. 一轮之内不查止损**：`b14f56b` + `a498af8`。一轮里每处理一个币前、目录回填后、持有人预热后，只要持仓且距上次检查满 15 秒就补查一次退出；间隔从上次检查结束算起，避免挤占采集。
- [x] **5. 一个价格源失败，整轮作废**：`3434e12` + `ddbde54`。只有 ETH 价格是必需的；稳定币报价失败不再让整轮失败，ETH 池照常标价止损。冻结规则不变：稳定币缺失、过期或脱锚时所有买入仍被挡住（且提前挡，不浪费 RPC），USDG 池暂不标价；没有时间戳的 USDG 报价视为缺失；挂单信号在此期间保留到 3 分钟寿命结束。
- [x] **6. 砸盘后仓位可能无法估值**：`b51e43f` + `3467e8b`。入场时存下的持有人都不够卖时，改用当前前 10 大持有人重试一次并存回仓位；每个仓位最多 10 分钟刷新一次，避免冻结仓位反复扫持有人历史。

## 以后可以考虑（扫描发现的次要问题，未修）

- 换座位时会补判离开期间的全部分钟（`evaluated_minutes` 无上限），污染筛选统计。
- 已暂停的 C 仍占持有人预热轮转。
- `ledger.mjs`、`paper.mjs` 是不再运行的旧代码。
